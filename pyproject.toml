[project]
name = "c64u-mcp-server"
version = "0.1.0"
description = "MCP server for Commodore 64 Ultimate Computer REST API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.25.0",
]

[project.scripts]
c64u-mcp-server = "server:main"
